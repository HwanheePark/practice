var hor = 4;
var ver = 3;
var color_candidate = [
    `red`, `red`, 
    `gold`, `gold`, 
    `springgreen`, `springgreen`, 
    `grey`, `grey`, 
    `green`, `green`, 
    `purple`, `purple`
];
var color = [];
for (var i = 0; color_candidate.length > 0; i++) {
    color = color.concat(color_candidate. )
}